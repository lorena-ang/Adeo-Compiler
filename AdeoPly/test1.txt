program test1;

Class Perro
{
	int: edad,
	string: color
};

var int: totalPerros;

void function printPerro(int edadPerro, string colorPerro)
{
	print("Edad", edadPerro);
	print("Color", colorPerro);
}

int function addYear(int edadPerro)
{
	edadPerro = edadPerro + 1;
	return edadPerro;
}

main()
{
	var string: frase1;
	var int: i, tempEdad, contEdad;
	var Perro: perro;

	print("Ingresa la edad del perro: ");
	read(perro.edad);
	perro.color = "rojo";

	frase1 = "El perro es un cachorro.";

	if (perro.edad < 1)
	{
		print(frase1);
	}
	elseif (perro.edad < 8)
	{
		print("El perro es un adulto.");
	}
	else
	{
		print("El perro es senior.");
	}

	for i = 1 to 5 do
	{
		totalPerros = totalPerros + 10;
	}

	tempEdad = perro.edad;
	contEdad = 0;

	while (tempEdad <= 20)
	{
		tempEdad = addYear(tempEdad);
		contEdad = contEdad + 1;
	}

	printPerro(perro.edad, perro.color);
}